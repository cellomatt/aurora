doctype html
html
  head
      block head
        //- title needs to be passed in on route handlers
        title Aurora - #{title}
        link(rel='stylesheet' href='/stylesheets/reset.css')
        link(rel='stylesheet' href='/stylesheets/index.css')
  body
    //- nav bar (including home button)
    //- div.nav_bar
    //-   a(href="/")
    //-     img(src="/images/logo.svg" width="150px")


    div.topnav
      a(href="/")
        img.logo(src="/images/logo.svg" width="150px")
      div.flex-containter
        a(href="/home") 🏠 Home
        a(href="/topics") 📄 Topics
        a(href="/answers") 📩 Answers
      input.search_bar(type="text" placeholder="What's your question?")
      select(name="topicId" class="topnav__dropdown" required)
          option Topic
      select(name="topicId" class="topnav__dropdown" required) Expertise
          option Expertise
      button.nav__submit_button Ask a Question!



  //- logo image
  block content
