extends layout.pug

block content
    h1=topic.name
    each question in questions
        div
            a(href=`/questions/${question.id}`)
                span
                    p=question.title
                